# Light Charity Backend - Production Environment Variables
# Copy this file to .env and fill in your actual values

# Core Configuration
NODE_ENV=production
PORT=8080

# Database Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/light-charity?retryWrites=true&w=majority

# Authentication Secrets (IMPORTANT: Use strong, unique secrets)
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_long_and_random
JWT_REFRESH_SECRET=your_super_secret_refresh_token_key_different_from_jwt_secret_also_32_chars_min

# Email Service (Resend)
RESEND_API_KEY=re_your_resend_api_key_here
EMAIL_FROM=Light Charity <noreply@yourdomain.com>

# AI Chatbot Service (Groq)
GROQ_API_KEY=your_groq_api_key_here

# Frontend URL (for CORS configuration)
FRONTEND_URL=https://your-app-name.vercel.app

# WebSocket Configuration
WEBSOCKET_HEARTBEAT_INTERVAL=30000
MAX_WEBSOCKET_CONNECTIONS=1000

# Optional Services
REDIS_URL=redis://username:password@redis-host:port/database
NEWS_API_KEY=your_news_api_key_here

# Security Notes:
# - JWT_SECRET and JWT_REFRESH_SECRET should be different and at least 32 characters
# - Use a secure random string generator for secrets
# - Never commit actual secrets to version control
# - Rotate secrets regularly in production

# Example secret generation (run in terminal):
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"